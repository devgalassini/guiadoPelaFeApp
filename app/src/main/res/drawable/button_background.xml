<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@color/primaryColor" /> <!-- Cor de fundo do botão -->
    <corners android:radius="20dp" /> <!-- Define o arredondamento dos cantos -->
    <padding
        android:left="12dp"
        android:top="8dp"
        android:right="12dp"
        android:bottom="8dp" /> <!-- Define o espaçamento interno -->
</shape>
